// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

// Detect when pizza list section is in view
//window.addEventListener("scroll", function () {
//    var pizzaListSection = document.getElementById("pizzaList");
//    var rect = pizzaListSection.getBoundingClientRect();

//    // If the section is in view, make it visible
//    if (rect.top <= window.innerHeight) {
//        pizzaListSection.classList.add("visible");
//    }
//});
