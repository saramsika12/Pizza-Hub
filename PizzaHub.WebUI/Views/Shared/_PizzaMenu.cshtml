<div class="container pb-4">
    <div class="row pt-4">
        <h2 class="col-sm-12 mb-4" id="heading">Pizza, Dessert, Beverages</h2>
    </div>
    <div class="row d-flex flex-wrap">
        @foreach (var item in Model)
        {
            <div class="col-sm-4 pb-4 d-flex">
                <div class="card box-shadow w-100">
                    <img class="card-img-top" src="@item.ImageUrl" alt="image" style="width:100%">
                    <div class="card-body d-flex flex-column">
                        <h4 class="card-title">
                            @item.Name 
                            <span class="float-right
                          font-20">Rs @item.UnitPrice.ToString("N2")</span>
                        </h4>
                        <p class="card-text">@item.Description</p>
                        <p>
                            <span class="pt-1 d-inline-block"> Size: Regular</span>
                            <span class="float-right font-20">
                                <a class="btn btn-primary" href="javascript:void(0)" onclick="AddToCart
                                 ('@item.Id','@item.Name','@item.UnitPrice',1)">
                                Add To Cart
                            </a>
                            </span>
                        </p>
                    </div>
                </div>
            </div>

        }
    </div>
</div>

<div class="toast fade hide" data-autohide="false" id="toastCart">
    <div class="toast-header">
        <strong class="mr-auto text-primary">Notification</strong>
        <button type="button" class="m1-2 mb-1 close" data-dismiss="toast" aria-label="Close">×</button>
    </div>
    <div class="toast-body"></div>
</div>

@* <script>
    function AddToCart(id, name, price, quantity) {
        // Example function logic
        console.log("Item added to cart:", { id, name, price, quantity });
        $('#toastCart .toast-body').text(`Added ${name} to cart!`);
        $('#toastCart').toast('show');
    }
</script> *@